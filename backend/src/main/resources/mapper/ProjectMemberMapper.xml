<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.durian.groupware.project.mapper.ProjectMemberMapper">

    <select id="findByProjectAndUser" resultType="ProjectMember">
        SELECT * FROM project_members
        WHERE project_id = #{projectId} AND user_id = #{userId}
    </select>

    <select id="findByProjectId" resultType="ProjectMember">
        SELECT * FROM project_members WHERE project_id = #{projectId}
    </select>

    <!-- 멤버 목록 응답용 — 사용자 이름과 부서명까지 JOIN해서 한 번에 가져온다 -->
    <select id="findMembersByProjectId" resultType="ProjectMemberResponse">
        SELECT pm.id, pm.project_id, pm.user_id,
               u.name AS user_name,
               d.name AS department_name,
               pm.role
        FROM project_members pm
        JOIN users u ON pm.user_id = u.id
        LEFT JOIN departments d ON u.department_id = d.id
        WHERE pm.project_id = #{projectId}
        ORDER BY pm.id
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO project_members (project_id, user_id, role)
        VALUES (#{projectId}, #{userId}, #{role})
    </insert>

    <update id="updateRole">
        UPDATE project_members SET role = #{role}
        WHERE project_id = #{projectId} AND user_id = #{userId}
    </update>

    <delete id="delete">
        DELETE FROM project_members
        WHERE project_id = #{projectId} AND user_id = #{userId}
    </delete>

</mapper>